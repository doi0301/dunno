# 다음 단계 진행 결과 (배포 + 리스크)

## 완료한 작업
- `azd init --minimal --no-prompt` 실행 완료
- `azure.yaml` 생성 완료
- Static Web Apps 서비스 설정 추가 완료

## 현재 상태
- 프로젝트는 azd 기준으로 완전 초기화됨
- 배포 준비 완료 상태

## 바로 이어서 할 일 (배포 직전 단계)
1. 환경 생성
   - `azd env new hack0620`
2. Azure 구독 선택 및 배포
   - `azd up`
   - 자동으로 Static Web Apps 생성 + 배포

## 리스크 체크리스트
- 이미지 소스(키 없는 엔드포인트) 503 가능성
  - 대응: 카드 이모지 폴백 유지 (현재 적용됨)
- 서비스 선택 미완료 시 `azd up` 실패
  - 대응: 반드시 `azd add` 후 `azd up`
- 해커톤 네트워크 불안정
  - 대응: 데모용 데이터(localStorage) 사전 주입 유지
- 커스텀 카테고리/이미지 데이터 용량 증가
  - 대응: 브라우저 localStorage 한도 고려, 이미지 업로드 4MB 제한 유지

## 데모 직전 점검
- 모바일에서 URL 접속 확인
- 위치별/카테고리별 필터 정상 동작
- 이미지 업로드/촬영/검색 + 폴백 확인
